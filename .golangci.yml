# For documentation, see https://golangci-lint.run/usage/configuration/

linters:
  enable:
    - errorlint
    - gofumpt
    - nolintlint
    - unconvert
    - unparam

linters-settings:
  govet:
    enable:
      - nilness
